<%= form_for(@outil) do |f| %>
  <% if @outil.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@outil.errors.count, "error") %> prohibited this outil from being saved:</h2>

      <ul>
      <% @outil.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br>
    <%= f.text_field :user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
